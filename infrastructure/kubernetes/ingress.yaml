apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ai-pr-reviewer
spec:
  rules:
    - host: ai-pr-reviewer.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: backend
                port:
                  number: 8000
